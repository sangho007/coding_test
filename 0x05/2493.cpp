#include <bits/stdc++.h>
using namespace std;

int main(){
    int N;
    cin >> N;
    int ans[500000] = {0};

    vector<int> v;

    while(N--){
        int t;
        cin >> t;
        v.push_back(t);
    }

    

    return 0;
}