#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s;
    cin >> s;

    
    stack<int> s;
    int t = 0;

    for ( auto c:s){
        if (c == '('){
            
        }
        if (c == '['){

        }
        if (c == ')'){

        }
        if (c == ']'){
            
        }
    }

    // cout << st_n.top();
    return 0;
}