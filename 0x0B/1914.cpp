#include <bits/stdc++.h>
using namespace std;

int main(){
    int k;
    cin >> k;

    stack<int> st1;
    stack<int> st2;
    stack<int> st3;

    while(k--){
        st1.push(k+1);
    }

    while(!st1.empty() || !st2.empty()){
        
    }
}